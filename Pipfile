[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
# Wagtail
wagtail = "*" # <2.15,>=2.14.1
# Django 
django = "*" # 3.2.8
whitenoise = {version = "*", extras = ["brotli"]} # Serve static files
django-strip-whitespace = {file = "https://codeload.github.com/baseplate-admin/django_strip_whitespace/zip/refs/heads/remove-compression"} # Minify html
# Servers
uvicorn = {version = "*", markers = "sys_platform != 'win32'", extras = ["standard"]}
gunicorn = {version = "*", markers = "sys_platform != 'win32'", extras = ["gevent","setproctitle"]} # To serve Gunicorn

[dev-packages]
black = "*"

[requires]
python_version = "3.10"

[pipenv]
allow_prereleases = true

[scripts]
# collectstatic = "python ./manage.py collectstatic_rewired"
dev = "python manage.py runserver"
prod = "gunicorn core.asgi:application -w 4 -k uvicorn.workers.UvicornWorker" # For linux only
